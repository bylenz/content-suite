## 1. Módulo `knowledge`: modelo y migración

- [x] 1.1 Crear `apps/api/app/knowledge/models.py` con `BrandKnowledgeChunk` (id UUIDv5 determinista PK, brand_id, brand_dna_version_id, section, rule_type, scope enum `TEXT|VISUAL|BOTH`, mandatory, content, atributo Python `chunk_metadata` mapeado a columna DB `metadata`, embedding, created_at) e índices (brand+versión, mandatory parcial por versión, scope por versión) según `DATA_MODEL.md` y `design.md`; añadir columnas nulables `knowledge_embedding_model`/`knowledge_embedding_dimensions`/`knowledge_fingerprint` a `BrandDnaVersion`; registrar el módulo en `alembic/env.py` y verificar con `uv run ty check`.
  - Resultado: `app/knowledge/models.py` con `KnowledgeScope`, `Embedding` (TypeDecorator dual dialecto: texto literal pgvector / JSON SQLite) y los tres índices; columnas de evidencia añadidas a `BrandDnaVersion`; `alembic/env.py` registra `app.knowledge.models`. `uv run ty check` → All checks passed.
- [x] 1.2 Crear la migración Alembic reversible de `brand_knowledge_chunks` + columnas de sync en `brand_dna_versions` con branch por dialecto: `CREATE EXTENSION IF NOT EXISTS vector` + columna `vector` sin dimensión en PostgreSQL, columna JSON en SQLite; verificar con round-trip `uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` sobre SQLite y documentando en la migración la limitación SQLite.
  - Resultado: migración `c4a9b2e6f8d1_brand_knowledge_chunks.py` (branch por dialecto documentado en su docstring; la extensión `vector` no se elimina en downgrade por ser facility compartida). Round-trip SQLite ejecutado: `upgrade head` → `downgrade -1` → `upgrade head`, todo OK.

## 2. Chunking determinista con IDs estables

- [x] 2.1 Crear `chunker.py` que transforme un `BrandDnaDocument` en chunks según el mapping canónico de `design.md` (sección, rule_type, scope, mandatory, content, metadata con order/field/index), asigne `id = uuid5(CHUNK_NAMESPACE, f"{version_id}|{order}|{field}|{index}")` y calcule el fingerprint SHA-256 del conjunto ordenado; verificar con tests unitarios de: determinismo (mismo documento → mismos chunks y mismos IDs), estabilidad de IDs a través del re-chunking, unicidad entre versiones/campos, cobertura total de unidades atómicas, flags mandatory exactos (avoid_vocabulary, dont_examples, communication.rules, logo_usage, restrictions), scopes TEXT/VISUAL/BOTH y fingerprint sensible al contenido.
  - Resultado: `app/knowledge/chunker.py` (`CHUNK_NAMESPACE` documentado, mapping canónico, `fingerprint_for`); `tests/test_knowledge_chunker.py` (8 tests) cubre determinismo, estabilidad, disyunción entre versiones, cobertura, mandatory exactos, scopes, fingerprint sensible al contenido, joins vs ítems → 8 passed.

## 3. Adapter OpenAI de embeddings (env-gated, aislado)

- [x] 3.1 Verificar con Context7/documentación oficial el SDK `openai` (versión estable compatible con Python 3.12 y Pydantic v2, incluidas las dimensiones por defecto documentadas de sus modelos de embedding y el parámetro `dimensions` de `text-embedding-3-*`) y documentar la decisión (versión y hallazgos) en esta tarea; solo entonces instalar con `uv add openai`. Si la verificación invalida el enfoque del adapter, registrar el hallazgo y el ajuste en esta misma tarea.
  - Decisión: Context7 (`/openai/openai-python`, oficial, reputación alta) documenta el cliente síncrono/async para Python 3.10+, `client.embeddings.create(input, model, dimensions)` con `CreateEmbeddingResponse.data[i]` portando `embedding: List[float]` e `index: int` que preserva el orden del input; dimensiones por defecto documentadas: `text-embedding-3-small` → 1536, `text-embedding-3-large` → 3072, `text-embedding-ada-002` → 1536 (fija); `dimensions` soportado en `text-embedding-3-*`. `uv add openai` resolvió **3.13.0** (Python 3.12). Verificado estáticamente en el entorno instalado: `OpenAI(api_key=...)` existe y `Embeddings.create` expone `input/model/dimensions`. Enfoque del adapter validado, sin ajustes.
- [x] 3.2 Extender aditivamente el puerto `EmbeddingModel` en `app/ai/ports.py` con `dimensions: int` (declarado desde configuración, sin llamada al proveedor) y `FakeEmbeddingModel.dimensions = EMBEDDING_DIMENSIONS` más el atributo `dimensions` en el doble de test 005 `FakeForTestEmbedding` (`apps/api/tests/test_ai_ports.py`): sin él, su asignación estática anotada al puerto falla `ty check` y su `isinstance` runtime falla (`runtime_checkable` exige presencia del atributo); crear `app/ai/providers/openai_embeddings.py` (`OpenAIEmbeddingModel` implementando `name`, `dimensions` y `async def embed`, SDK importado solo dentro del archivo, llamadas bloqueantes envueltas con `asyncio.to_thread`) y `resolve_embedding_model(settings)` en `app/ai/providers/__init__.py` que devuelve el adapter solo con `ai_provider="openai"` + `openai_api_key` + dimensión cognoscible sin red (setting explícita o modelo en la tabla documentada de defaults) y `None` con un único log sanitizado (nombres de variables faltantes, nunca valores) en configuración ausente o parcial; añadir settings `openai_api_key`/`openai_embedding_model`/`openai_embedding_dimensions` (vacías/0 por defecto) a `config.py`; verificar con tests usando stub del SDK (sin red): batching correcto, `name`/`dimensions` declarados sin red, errores del SDK → excepción explícita, resolución ausente/parcial/completa (incluido modelo desconocido sin dimensión explícita → `None`) y que ningún otro módulo importa el SDK; verificar que la suite 005 sigue pasando con comportamiento intacto — única edición en archivos 005: `FakeEmbeddingModel` y el doble `FakeForTestEmbedding` ganan el atributo declarado, conservando sus pruebas de compatibilidad estática y runtime; ningún test de comportamiento cambia.
  - Resultado: puerto extendido con `dimensions: int`; `FakeEmbeddingModel.dimensions = 8` y `FakeForTestEmbedding.dimensions = 1` (compatibilidad estática + runtime verificada por `ty check` e `isinstance`); `openai_embeddings.py` (SDK lazy, `asyncio.to_thread`, orden por `index`, `dimensions` solo en `text-embedding-3-*`) y `resolve_embedding_model` (caché por estado de config, tabla de defaults documentada, modelo desconocido sin dimensión → `None`, ada-002 solo acepta 1536); settings añadidas. Tests: `tests/test_openai_embeddings.py` (14, stub del SDK sin red) + suite 005 (`test_ai_ports`, `test_ai_fakes`, `test_import_audit`, `test_settings_ai`) → all passed. Ediciones en archivos 005: los dos dobles ganan `dimensions` (exigido por esta tarea) y `test_import_audit.test_ai_providers_package_contains_no_sdk_imports_yet` → actualizado a la lista esperada de archivos (assert de contenido de directorio que esta change extiende por diseño; reglas de auditoría sin cambios). Auditoría de imports sigue verde: OpenAI solo en `ai/providers/`.
  - Corrección post-review (P1): el resolver ahora acepta una dimensión reducida explícita y positiva para `text-embedding-3-*` (parámetro `dimensions` del proveedor; p. ej. small=512, large=256), conservando la dimensión fija exigida para `text-embedding-ada-002` y el rechazo de modelos desconocidos sin dimensión explícita. Tests añadidos: reduced dims 3-small/3-large resuelven adapter; ada-002 con 1536 documentada resuelve adapter (mismatch 512 → `None` ya cubierto).

## 4. Repositorio con dual dialecto

- [x] 4.1 Crear `repository.py` con: lectura de chunks por marca+versión con filtros mandatory/scope, reemplazo transaccional por versión (delete+insert preservando los UUIDv5), conteo por versión, y ranking semántico con branch por dialecto (`embedding <=> :q` en PostgreSQL, coseno en Python solo SQLite como seam de test documentado), ambos con orden total determinista (distancia ASC, `id` ASC); verificar con tests de: aislamiento entre marcas y versiones (dos marcas/versiones sincronizadas nunca se mezclan), reemplazo idempotente (mismos IDs tras reemplazo), filtros mandatory/scope, ranking determinista y desempate por `id` en SQLite.
  - Resultado: `app/knowledge/repository.py` (`list_chunks` con filtros, `count_chunks`, `replace_version_chunks` delete+insert con `strict=True`, `rank_semantic` con branch por dialecto — PG usa `embedding <=> CAST(:q AS vector)` y SQLite coseno en Python, ambos con orden `(distancia, id)`). `tests/test_knowledge_repository.py` (5 tests): aislamiento marca/versión, reemplazo idempotente con mismos IDs, filtros, empate exacto desempatado por `id` ASC, orden por distancia y no-fuga entre versiones → 5 passed.
  - Corrección post-review (P1, rama PostgreSQL): (1) `scope IN :scopes` usa ahora `bindparam(expanding=True)` — la tupla plana nunca se expande y PostgreSQL no puede compararla contra el enum `knowledge_scope`; los valores viajan como strings de enum que la comparación coerciona. (2) `Embedding` es ahora genuinamente dual dialecto: `load_dialect_impl` resuelve `_PgVector` (`UserDefinedType` sin paquete pgvector: DDL `VECTOR`, writes/reads en texto literal pgvector) en PostgreSQL y `JSON` en SQLite — antes reclamaba VARCHAR en PostgreSQL. Verificación offline (sin PG vivo): `tests/test_knowledge_postgres_sql.py` (6 tests) compila contra el dialecto `postgresql` y afirma binding expansivo (`__[POSTCOMPILE_scopes]`, no `IN (%(scopes)s)`), valores enum planos, filtro antes de orden con `(distance, id)`, DDL `VECTOR` vs `JSON` y procesadores bind/result en texto literal pgvector; comportamiento SQLite intacto (suite repository + sync + retrieval siguen en verde).

## 5. Trazabilidad: extensión aditiva del contrato 005

- [x] 5.1 Extender aditivamente `app/observability/ports.CapabilitySpan` con `operation: str | None = None` y `prompt_version: str | None = None`, validando en construcción la invariante de nombre (al menos uno de `operation`/`prompt_version`; ambos ausentes → error), y `app/ai/contracts.TraceSummary` con contratos `"KnowledgeSync" | "KnowledgeEmbed" | "KnowledgeRetrieval"`, defaults `0` para `check_count`/`finding_count` y campos opcionales `chunk_count`/`mandatory_count`/`semantic_count` (0–10000), con validadores (conteos Knowledge solo en contratos Knowledge; contratos Knowledge dejan check/finding en 0) y `extra="forbid"` conservado; actualizar el adapter Langfuse para nombrar la observación con `span.operation or span.prompt_version` e incorporar `operation` al metadata; verificar que toda la suite 005 existente pasa sin cambios y con nuevos tests de: los valores Knowledge, las reglas de validación, el nombre de observación para spans con `prompt_version` (005) y para las tres operaciones Knowledge, y el rechazo de un span con ambos identificadores ausentes.
  - Resultado: `CapabilitySpan` reordenado (construcción por keyword de 005 intacta) con invariante de nombre en `__post_init__`; `TraceSummary` extendida (validadores de conteos Knowledge + `_StrictOmitNone` serializador que omite Nones, manteniendo los dumps 005 byte-idénticos); Langfuse nombra con `operation or prompt_version` y añade `operation` al metadata solo cuando existe. Tests: `tests/test_knowledge_tracing.py` (8) + suite 005 completa (`test_langfuse_adapter`, `test_observability_tracer`, `test_ai_contracts`, `test_ai_runner`) → all passed sin edición de tests 005 en esta tarea.

## 6. Sync service: claim atómico, finalización y fallo

- [x] 6.1 Crear `service.sync_knowledge` (async) con el orden de validación de `design.md`: membresía + rol CREATOR (403) → resolver ACTIVE (404) → check de presencia del adapter antes de cualquier mutación (`None` → `AIProviderNotConfiguredError` → 503 sin transición) → chunking determinista y fingerprint del documento ACTIVE antes del claim (CPU puro, sin transacción abierta ni `await`; modelo/dimensión esperados = los declarados por el adapter desde su configuración) → claim condicional atómico en transacción corta comprometida antes de cualquier `await` (fuente: `NOT_SYNCED|FAILED`, o `SYNCED` con desajuste de modelo/dimensión/fingerprint evaluado con los tres valores pre-computados; `rowcount=0` → releer: `SYNCED` sin desajuste → 200 idempotente, `SYNCING` → 409, `OUTDATED` → 409) → `await embed` batch sin transacción abierta → validación del lote (cardinalidad, floats finitos, dimensión única e igual a la declarada por el adapter, norma > 0) → finalización transaccional (delete+insert + `SYNCED` + modelo/dimensión/fingerprint) → fallo: rollback + `FAILED` en transacción separada con chunks previos intactos; spans `knowledge.sync` y `knowledge.embed` en éxito y error; verificar con tests de: cada transición, idempotencia (SYNCED sin desajuste de los tres valores no re-embebe), regeneración por cambio de modelo, por cambio de dimensión y por cambio de fingerprint, 503 sin cambio de estado, lote inválido (NaN/Inf, dimensiones mezcladas, dimensión distinta de la declarada, vector nulo) → FAILED sin persistencia parcial, OUTDATED → 409 sin transición, fallo transaccional con chunks previos intactos, spans (conteos, tres operaciones, sin payloads crudos) y un test concurrente genuino (dos tareas async con embed que cede el control: exactamente un 200, un 409 inmediato y un único conjunto de chunks).
  - Resultado: `service.sync_knowledge` implementado con el orden exacto (claim UPDATE condicional con `is_distinct_from` sobre los tres valores, commit inmediato, embed sin transacción abierta, finalización transaccional con UPDATE guardado por `SYNCING`, FAILED en transacción separada). `tests/test_knowledge_service.py`: transición NOT_SYNCED→SYNCED con UUIDv5/evidencia persistidos, idempotencia sin re-embed, regeneración por modelo/dimensión/fingerprint, 503 sin transición, 6 lotes inválidos parametrizados → FAILED sin persistencia parcial, OUTDATED→409 `outdated_needs_publish` sin embed, fallo de embed → FAILED con chunks previos intactos, spans éxito/error (conteos, sin contenido), concurrencia genuina con `YieldingEmbedding` (engine WAL dedicado): exactamente un ok + un `SyncConflictError`, un único conjunto de chunks. 37 passed.
- [x] 6.2 Añadir `KnowledgeNotAvailableError` a `knowledge/errors.py` (sin handler HTTP en esta change; su 503 lo registra el boundary de Creative, spec 04) y verificar con un test que ninguna ruta actual la mapea.
  - Resultado: `app/knowledge/errors.py` define `KnowledgeNotAvailableError` (y `SyncConflictError` con `details.reason` distinguibles). Test `test_knowledge_not_available_error_has_no_http_handler` verifica que `app.exception_handlers` no la registra.

## 7. Context builder híbrido fail-safe

- [x] 7.1 Crear `service.build_context` (async): adapter `None` → `KnowledgeNotAvailableError` (sin degradación a mandatory-only) → resolver ACTIVE (sin ACTIVE → error) → estado servible `SYNCED|OUTDATED` (`NOT_SYNCED|SYNCING|FAILED` → error) → compatibilidad de espacio vectorial antes de rankear (modelo/dimensión persistidos NULL o distintos de los declarados por el adapter de consulta → error) → mandatory del scope siempre presente (vacío → error fail-safe) → `await embed([query])` con fallos del adapter convertidos a `KnowledgeNotAvailableError` y validación del vector de consulta (cardinalidad 1, floats finitos, norma > 0, dimensión igual a la persistida; violación → error) → top-k semántico (k 1–20, default 8) filtrado por `brand_id`+`brand_dna_version_id` antes de rankear, segmentado `TEXT`∪`BOTH` / `VISUAL`∪`BOTH`, orden total (distancia, `id`) → span `knowledge.retrieve`; devolver `BuiltContext` (versión + `KnowledgeRule` con `id`, section, rule_type, scope, content) para que spec 04 persista `applied_rule_ids`; verificar con tests de: mandatory siempre incluido sin depender de similarity, aislamiento marca/versión en el top-k, segmentación por tarea, fail-safe por estado no servible, fail-safe sin versión ACTIVE, fail-safe sin mandatory aplicables, fail-safe con adapter de consulta ausente, fail-safe por cambio de modelo o de dimensión del adapter respecto a lo persistido (incluido NULL), fail-safe con vector de consulta malformado (cero o varios vectores, NaN/Inf, norma 0, dimensión distinta) y con fallo del adapter en la consulta, OUTDATED servible y estabilidad de IDs devueltos.
  - Resultado: `service.build_context` con el orden fail-safe exacto (compatibilidad de espacio vectorial con NULL/desajuste antes de rankear; conversión de fallos/validación del vector de consulta; clamp de top_k a 1–20) devolviendo `BuiltContext`/`KnowledgeRule` con IDs estables. Tests: mandatory sin depender de similarity, aislamiento marca/versión, segmentación TEXT/VISUAL/BOTH, 3 estados no servibles parametrizados, sin ACTIVE, sin mandatory aplicables (corrupción), adapter ausente, desajuste modelo/dimensión/NULL, 5 vectores de consulta malformados + fallo del adapter, OUTDATED servible, estabilidad de IDs, clamp de top_k y span `knowledge.retrieve` con conteos.

## 8. Endpoints con RBAC, membership-first y envelope

- [x] 8.1 Crear `router.py`, `schemas.py` y `policies.py` (lectura: cualquier rol con membresía; sync: solo CREATOR; membresía resuelta antes de cualquier lookup de marca/versión) con `GET /brands/{brand_id}/brand-knowledge` (chunks de la ACTIVE con `id`, sin embeddings), `GET .../brand-knowledge/status` (incluye `embedding_model`) y `POST .../brand-knowledge/sync`; registrar en `main.py` y handlers nuevos en `errors.py` (`SyncConflictError → 409` con detalles distinguibles concurrente vs OUTDATED, `AIProviderNotConfiguredError → 503` sanitizado); verificar con tests de integración: matriz RBAC (Creator/Content Reviewer/Visual Reviewer/sin membresía, incluido 403 con marca inexistente para no-miembro), 404 sin ACTIVE en los tres endpoints, 409 concurrente y 409 OUTDATED, 503 proveedor ausente sin cambio de estado, códigos del envelope y que reviewers solo ven datos de la versión publicada.
  - Resultado: `router.py` (dependencias `get_embedding_adapter`/`get_tracer_adapter` desde settings, override en tests con `FakeEmbeddingModel` sin red), `schemas.py` (`ChunkOut` con alias `chunk_metadata`→`metadata`, `KnowledgeChunksOut`, `KnowledgeStatusOut`), `policies.py` (lectura cualquier rol, sync solo CREATOR), registro en `main.py`, handlers `SyncConflictError→409` (reason distinguible) y `AIProviderNotConfiguredError→503` en `errors.py`. `tests/test_knowledge_router.py` (14 tests): lectura por los tres roles sin embeddings, status con/sin evidencia, matriz RBAC de sync, 403 no-miembro con marca inexistente en los tres endpoints, 404 sin ACTIVE en los tres, 409 concurrente genuino (un 200 + un 409 `sync_in_progress` vía ASGI con engine WAL), 409 OUTDATED (`outdated_needs_publish`) sin transición, 503 proveedor ausente sin cambio de estado ni chunks, reviewers sin datos de borrador.

## 9. Frontend: estado real y sync

- [x] 9.1 Actualizar `KNOWLEDGE_STATUS_VIEW` y copys de Brand DNA/Dashboard para reflejar los cinco estados reales (incluidos SYNCING y FAILED; OUTDATED explicado como borrador pendiente de publicar, con sync deshabilitado y ese motivo) sin prometer sincronización automática; añadir en la página de Brand DNA la acción "Sincronizar Knowledge" visible solo para CREATOR sobre la versión ACTIVE, llamando a `POST /brands/{id}/brand-knowledge/sync` con TanStack Query (mutación + invalidaciones) y mostrando errores del envelope (403/404/409/503) inline; verificar con `npm run lint`, `npm run typecheck`, `npm run build` en `apps/web` y revisión de que no se habilita nada de Creative.
  - Resultado: `statusView.ts` con los cinco estados reales y OUTDATED como "Borrador pendiente de publicar" (Dashboard hereda los labels); `BrandDnaPage` añade "Sincronizar Knowledge" (solo CREATOR, sobre ACTIVE; deshabilitada en OUTDATED con motivo visible y durante SYNCING) vía `useSyncKnowledge` (mutación + invalidación `['brand-dna', brandId]`, errores del envelope inline con feedback animado); `types.ts` añade `KnowledgeStatusOut`. `npm run lint` → sin errores; `npm run typecheck` → OK; `npm run build` → built (warning de chunk >500 kB preexistente). Sin pantallas nuevas ni nada de Creative.

## 10. Verificación

- [x] 10.1 Ejecutar `uv run ruff check`, `uv run ty check` y `uv run pytest` en `apps/api`; confirmar cero llamadas de red en tests (FakeEmbeddingModel/stub del SDK), que no cambian flujos de Brand DNA ni workflows existentes (regeneración desde SYNCED documentada y testeada como mantenimiento de datos derivados), y auditoría estática de imports (ningún SDK de proveedor fuera de `ai/providers/`: OpenAI solo en `openai_embeddings.py`, `langfuse` solo en su adapter).
  - Resultado: ruff `All checks passed!`; ty `All checks passed!`; pytest **227 passed** (2 warnings preexistentes starlette/httpx; incluye 6 tests offline de forma PostgreSQL y 2 nuevos del resolver de dimensiones). Cero red: el SDK OpenAI solo vía stubs inyectados y la lógica de sync/retrieval usa `FakeEmbeddingModel`; `_mark_active_outdated` y los flujos de publish de `brand_dna` no se tocaron (regeneración desde SYNCED testeada como mantenimiento de datos derivados). Auditoría: test AST `tests/test_import_audit.py` passed + comando manual `grep -rnE "^(import|from) (langfuse|openai|anthropic|google)" app --include='*.py' | grep -v langfuse_adapter | grep -v ai/providers/` → salida vacía (exit 1 del último grep = sin violaciones).
- [x] 10.2 Documentar en esta tarea la verificación manual de PostgreSQL/pgvector (pasos: `CREATE EXTENSION vector` implícito en migración, `alembic upgrade head` contra la DATABASE_URL de Supabase, sync real con adapter OpenAI configurado y consulta `<=>`), dejando claro que CI solo cubre SQLite; registrar el resultado o el pendiente sin marcarla completada si no no se ejecutó.

  **Ejecutado 2026-09-14** contra el proyecto Supabase real (`ContentSuite`, sa-east-1) durante la verificación manual de la change `009-content-governance`: `alembic upgrade head` corrió limpio (pgvector ya disponible, sin `CREATE EXTENSION` manual necesario); `POST /brands/{id}/brand-knowledge/sync` con el adapter OpenAI real (`text-embedding-3-small`) produjo 17 chunks reales y `knowledge_status=SYNCED`. **Bug real encontrado y corregido**: `_rank_semantic_sql` (`app/knowledge/repository.py`) comparaba `scope IN :scopes` contra la columna enum `knowledge_scope`; contra Postgres real esto falla con `operator does not exist: knowledge_scope = character varying` porque psycopg3 declara el bindparam expansivo con tipo `::VARCHAR` explícito, bloqueando la coerción implícita que el comentario original asumía. CI (solo SQLite) y los tests offline de forma de SQL (`tests/test_knowledge_postgres_sql.py`, compile-only) nunca ejercitaron este camino. Fix: `scope::text IN :scopes` (cast de columna, no de bindparams); tests offline actualizados a la nueva forma; generación de contenido con contexto RAG real verificada end-to-end tras el fix (recuperación semántica con `<=>` contra pgvector real, ranking determinista).
  - Pendiente (no ejecutada; tarea dejada sin completar según su propia condición): verificación manual en PostgreSQL/Supabase pendiente de credenciales. Pasos a ejecutar cuando estén disponibles: (1) la migración `c4a9b2e6f8d1` ejecuta `CREATE EXTENSION IF NOT EXISTS vector` y crea la columna `vector` sin dimensión — correr `CONTENT_SUITE_DATABASE_URL=postgresql+psycopg://... uv run alembic upgrade head`; (2) configurar `CONTENT_SUITE_AI_PROVIDER=openai`, `CONTENT_SUITE_OPENAI_API_KEY` (y modelo/dimensión si se desea) y ejecutar un sync real `POST /api/v1/brands/{id}/brand-knowledge/sync`; (3) confirmar el ranking pgvector con una consulta `ORDER BY embedding <=> CAST(:q AS vector), id` sobre `brand_knowledge_chunks`. CI/tests solo cubren SQLite (seam documentado); la rama PostgreSQL del repository (`_rank_pg`) no se ejecuta en CI.
- [x] 10.3 Ejecutar `npx @fission-ai/openspec@latest validate 006-brand-knowledge-rag --strict` y `git diff --check`; dejar la change sin archivar.
  - Resultado: `Change '006-brand-knowledge-rag' is valid`; `git diff --check` limpio; nada en staging; round-trip Alembic SQLite repetido (`upgrade head` → `downgrade -1` → `upgrade head`) OK; change sin archivar (14/15, solo queda 10.2 pendiente de credenciales PostgreSQL). Re-validado tras las correcciones P1 de rama PostgreSQL (binding expansivo de scopes, tipo `vector` dual dialecto, dimensiones reducidas 3-*): mismas verificaciones en verde, sin PG vivo.
