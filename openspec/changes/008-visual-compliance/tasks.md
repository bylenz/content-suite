## 1. Prerrequisitos y setup

- [ ] 1.1 Confirmar que las changes 004 (Creative Studio, spec 04), Content Governance (spec 05) y 007 (RLS hardening) están implementadas y mergeadas: existen `app/creative`, `app/governance` (o módulos equivalentes de 04/05), `creative_items.workflow_status` con `CONTENT_APPROVED`/`PENDING_VISUAL_REVIEW`/`VISUAL_CHANGES_REQUESTED`/`FINAL_APPROVED` y tabla `workflow_events`; registrar el head de migraciones vigente que será `down_revision` de esta change. Verificar con `uv run pytest` verde en `apps/api` y `git log` del head de migraciones.
- [ ] 1.2 Verificar con Context7/documentación oficial el cliente de Supabase Storage (versión estable compatible con el stack, API de upload privado y `create_signed_url` con expiración) e instalarlo con `uv add` en `apps/api`; registrar versión, fuentes y el comando exacto en esta tarea. Verificar con `uv sync` y `uv run python -c "import <paquete>"`.
- [ ] 1.3 Añadir settings de storage y visual audit en `app/config.py` (`CONTENT_SUITE_STORAGE_*`: project URL, service key, bucket, TTL de signed URL default 300s, tamaño máximo default 5MB, tipos permitidos) siguiendo el patrón de settings de 005/006, con defaults seguros y sin secretos hardcodeados; añadir `.env.example`. Verificar con test de settings y `uv run pytest tests/test_settings*.py`.

## 2. Migración

- [ ] 2.1 Crear `apps/api/alembic/versions/<rev>_visual_compliance_tables.py` con `down_revision` = head vigente (de 1.1): tablas `visual_assets` (id, creative_item_id FK, version int, storage_path text, metadata jsonb, uploaded_by FK, created_at; `UNIQUE(creative_item_id, version)`), `visual_audits` (id, visual_asset_id FK, brand_dna_version_id FK, checks jsonb, findings jsonb, score numeric, summary text, applied_context jsonb, langfuse_trace_id text nullable, created_at), `visual_reviews` (id, visual_audit_id FK, reviewer_id FK, decision enum(APPROVED, CHANGES_REQUESTED), feedback text nullable, exception_accepted boolean default false, evidence jsonb nullable, created_at; constraint único `(visual_audit_id, reviewer_id, decision)`), índices por `creative_item_id` y `visual_asset_id`; en PostgreSQL además RLS enabled + policies `FOR SELECT TO authenticated` member-scoped con el mismo predicado EXISTS sobre `brand_memberships` del patrón 007 (reutilizando helper si 007 lo extrajo) y sin policies de escritura; rama SQLite no-op documentada; downgrade reversible completo. Verificar con `uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` sobre SQLite y `uv run ty check`.
- [ ] 2.2 Crear `apps/api/tests/test_visual_compliance_migration_sql.py` (patrón `test_rls_migration_sql.py` de 007, sin red ni instancia) verificando la rama PostgreSQL: RLS y policies solo en las tres tablas nuevas, todas `FOR SELECT TO authenticated`, sin policies de escritura, constraints únicos presentes y downgrade espejo. Verificar con `uv run pytest tests/test_visual_compliance_migration_sql.py`.

## 3. Módulo storage

- [ ] 3.1 Crear `apps/api/app/storage/` con puerto (`put_object`, `signed_url`, `remove_object`), errores propios (`StorageNotConfiguredError`, `StorageValidationError`, `StorageUnavailableError`) y `FakeStorage` determinista para tests (in-memory, con expiración simulable). Verificar con `uv run pytest` tras los tests de 3.2 y `uv run ty check`.
- [ ] 3.2 Implementar validación server-side en Python puro: límite de tamaño y detección de tipo real por magic bytes (PNG/JPEG/WebP según design D3), rechazo 422 antes de crear registros; test que cubre content type falsificado, tamaño fuera de límite y ausencia de huérfanos en el fake al fallar la transacción posterior. Verificar con `uv run pytest tests/test_storage_validation.py`.
- [ ] 3.3 Implementar `storage/providers/supabase_storage.py` con el SDK importado solo ahí (upload privado a ruta server-side `brands/{brand_id}/creative-items/{item_id}/visuals/v{n}{ext}`, `create_signed_url` con TTL de settings, remove para compensación) y resolver env-gated que devuelve `None` con un único log sanitizado si la configuración es parcial; registrar handlers de errores en `app/errors.py` (503 storage). Verificar con test del resolver (stub del SDK, sin red) y auditoría de imports: `grep -rn "supabase" apps/api/app --include="*.py"` solo encuentra el provider y config.

## 4. Dominio visual_audit (modelos, contratos, políticas)

- [ ] 4.1 Crear `apps/api/app/visual_audit/models.py` y `schemas.py`: modelos de `visual_assets`/`visual_audits`/`visual_reviews`, contratos Pydantic `VisionFinding` (`rule_id/category/severity HIGH|MEDIUM|LOW/status PASS|FAIL/expected/detected/evidence/recommendation`, `extra="forbid"`), response schemas sin `storage_path` y con `signed_url` computada. Verificar con tests de contrato: finding inválido rechazado, response serializa signed URL y nunca path interno (`uv run pytest tests/test_visual_contracts.py`).
- [ ] 4.2 Implementar `visual_audit/policies.py` con `compute_score(findings)` pura y determinista (HIGH 25 / MEDIUM 10 / LOW 3 por finding FAIL, floor 0), `require_content_approved_state`, `require_visual_reviewer` y regla de excepción HIGH (`approve requiere exception_accepted si hay HIGH fail`); test determinista (mismos findings → mismo score, dos ejecuciones) y de permiso/transición inválida. Verificar con `uv run pytest tests/test_visual_policies.py`.

## 5. Servicios de dominio

- [ ] 5.1 Implementar upload de visual en `visual_audit/service.py`: membership-first (403), item de la marca (404), estado `CONTENT_APPROVED` o `VISUAL_CHANGES_REQUESTED` (409), validación storage (422), upload a ruta server-side, inserción de nueva versión (`UNIQUE(creative_item_id, version)`) y transición de workflow + `workflow_event` en una transacción (primer visual → `PENDING_VISUAL_REVIEW`; corrección reabre `PENDING_VISUAL_REVIEW`), con compensación `remove_object` si la transacción falla; inmutabilidad: sin UPDATE de assets versionados. Verificar con tests de servicio (fake storage + fake repo): happy path, 403 sin membresía, 409 estado inválido, 422 archivo inválido sin huérfanos, corrección crea v2 sin mutar v1.
- [ ] 5.2 Implementar auditoría en el service: invocación del `VisionModel` solo desde el service, prompt desde el prompt registry (`ai/prompts.py`, versión propia), contexto visual via Knowledge (mandatory + semántico VISUAL/BOTH) con fail-safe → `KnowledgeNotAvailableError` mapeado a 503 sin llamar al modelo ni persistir auditoría; validación Pydantic del output → 503 `VISION_OUTPUT_INVALID` sin persistir; score computado con D5 y auditoría insert-only referenciando asset exacto + `brand_dna_version_id` + conteo de reglas; re-audit crea registro nuevo. Verificar con tests: audit happy (fake Vision + fake knowledge), 503 sin mandatory, 503 output inválido, re-audit no muta previas.
- [ ] 5.3 Implementar decisiones en el service: solo `VISUAL_REVIEWER` (403), auditoría de la marca (404), approve con HIGH sin `exception_accepted` → 422; approve con excepción persiste evidencia (ids/severity de los HIGH); approve → `FINAL_APPROVED`, request-changes → `VISUAL_CHANGES_REQUESTED`, ambos con `workflow_event` en la misma transacción; decisiones insert-only e idempotentes (misma decisión+payload devuelve la existente, sin duplicados). Verificar con tests: 403 creator/reviewer de contenido, 422 excepción, approve happy con evento, request-changes exige versión nueva, retry idempotente.

## 6. API y trazabilidad

- [ ] 6.1 Implementar `visual_audit/router.py` con los endpoints de `API.md`: `GET /visual-reviews/queue`, `POST|GET /creative-items/{item_id}/visual-assets`, `POST|GET /visual-assets/{asset_id}/audit`, `POST /visual-audits/{audit_id}/approve|request-changes`, `GET /creative-items/{item_id}/visual-audit-history`; membership-first en todos, signed URLs computadas en el boundary, envelopes 403/404/409/422/503 y montaje en `main.py`. Verificar con tests de integración API por endpoint (statuses, envelope shape, sin storage_path en responses).
- [ ] 6.2 Añadir spans sanitizados `visual.upload` y `visual.audit` vía el puerto `Tracer` (identidad de asset/versión, versión Brand DNA, modelo/prompt version, latencia, conteos y score; sin imágenes, prompts ni findings completos) y persistir `langfuse_trace_id` en la auditoría; fallo del tracer no rompe la operación. Verificar con test del fake tracer siguiendo el patrón de `test_knowledge_tracing.py`.

## 7. Frontend

- [ ] 7.1 Crear `apps/web/src/features/brand-audit`: cola del Visual Reviewer, detalle de asset con imagen via signed URL (nunca path), lista de findings/evidencia con severidades, historial de auditorías y decisiones, y panel de decisión (approve con checkbox de excepción HIGH y confirmación, request-changes con feedback); estados de carga/error/vacío reales mapeando 403/409/422/503. Verificar con `npm run typecheck` y `npm run build` en `apps/web`.
- [ ] 7.2 Extender Creative Studio (feature de la spec 04, aditivo): upload de visual para items `CONTENT_APPROVED`/`VISUAL_CHANGES_REQUESTED`, listado de versiones con estado y feedback del reviewer; habilitar la entrada "Brand Audit" del nav solo para `VISUAL_REVIEWER` según `nav.ts` y `docs/UI_UX.md`; preservar Dashboard/Brand DNA aprobados. Verificar con `npm run typecheck`, `npm run build` y revisión manual de nav por rol con dev tokens.
- [ ] 7.3 Validar la UI contra `starter-design/Content Suite.dc.html` y `docs/UI_UX.md`: clay fuerte en cola/decisión, superficies editoriales tenues para findings, paleta canónica y contraste AA; no introducir pantallas fuera del alcance de esta change. Verificar con revisión visual manual y `npm run lint` limpio.

## 8. Calidad y validación final

- [ ] 8.1 Ejecutar `uv run ruff check`, `uv run ty check` y `uv run pytest` en `apps/api` (suite completa sin modificar tests de 004–007) y `npm run lint`, `npm run typecheck`, `npm run build` en `apps/web`; auditoría de imports (SDK storage solo en provider, VisionModel solo invocado desde `visual_audit/service.py`). Registrar resultados en esta tarea.
- [ ] 8.2 Ejecutar `npx @fission-ai/openspec@latest validate 008-visual-compliance --strict` y `git diff --check`; dejar la change sin archivar.
- [ ] 8.3 Verificación manual end-to-end (documentar resultado o pendiente sin marcar si no se ejecutó): con dev tokens, como Creator completar un item hasta `CONTENT_APPROVED` (flujos 04/05), subir visual permitido, ejecutar audit (Vision fake/configurado), como Visual Reviewer pedir cambios, subir nueva versión como Creator y aprobar como Visual Reviewer (con y sin excepción HIGH); intentar rechazo de archivo inválido y acceso sin membresía esperando 422/403; verificar que ninguna URL pública del bucket funciona.
