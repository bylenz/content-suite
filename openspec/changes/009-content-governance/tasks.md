## 1. Módulo `governance`: modelos y migración

- [x] 1.1 ANTES de escribir, verificar contra la migración vigente de `008-creative-studio` (cadena Alembic actual con `uv run alembic history`) si `creative_items.workflow_status` ya existe; documentar el hallazgo en esta tarea. Luego crear `apps/api/app/governance/models.py` con `ContentReview` (id, creative_item_id, submitted_version_id, reviewer_id, decision enum `APPROVED|CHANGES_REQUESTED`, feedback nullable, created_at; UNIQUE(creative_item_id, submitted_version_id, decision)) y `WorkflowEvent` (id, creative_item_id, event_type text, actor_id nullable FK a profiles, metadata jsonb, created_at; índice (creative_item_id, created_at)) según `DATA_MODEL.md` y `design.md`; añadir `CreativeWorkflowStatus` (DRAFT, PENDING_CONTENT_REVIEW, CONTENT_CHANGES_REQUESTED, CONTENT_APPROVED) y `workflow_status` en el modelo de creative item SOLO si 008 no lo creó; registrar el módulo en `alembic/env.py` y verificar con `uv run ty check`.

  **Hallazgo:** `creative_items.workflow_status` y `workflow_events` YA existían (migración `e1f4a7c9d3b2`, Creative Studio) con el enum `creative_workflow_status` completo (7 estados, incluidos los visuales para 008). `app/governance/models.py` define solo `ContentReview`/`ContentReviewDecision`; `WorkflowEvent`/`workflow_status` siguen siendo de `app.creative.models` y se reutilizan vía `app/governance/repository.py`. `uv run ty check` limpio sobre el módulo nuevo.
- [x] 1.2 Crear la migración Alembic reversible de `content_reviews` + `workflow_events` + `creative_items.workflow_status` (columna, enum `creative_workflow_status` con default `DRAFT` NOT NULL e índice parcial de cola sobre `PENDING_CONTENT_REVIEW`) según el hallazgo de 1.1, respetando la cadena de revisión vigente; verificar round-trip `uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` sobre SQLite.

  **Nota:** la cadena tenía dos heads (`a3d8f2c6e9b1` observability, `e3dfa0780566` RLS TRUNCATE-revoke) anticipados por el propio docstring de `e3dfa0780566` ("a later merge migration reconciles multiple heads"). Se añadió el merge no-op `a73adfcae836` antes de `content_reviews` (`a31264bda9ea`). Round-trip verificado sobre SQLite; aplicar a Supabase queda pendiente de confirmación explícita (ver 5.1).

## 2. Policies, repository y errores

- [x] 2.1 Crear `apps/api/app/governance/policies.py` con `InvalidReviewTransitionError` (409) y guards de rol por command: submit exige `CREATOR`; approve/request-changes exigen `CONTENT_REVIEWER`; lecturas de cola exigen `CONTENT_REVIEWER` y detalle/history admiten `CREATOR`/`CONTENT_REVIEWER` de la marca; reutilizar `authorize_brand_action` de Identity. Crear `apps/api/app/governance/repository.py` con: claim condicional atómico sobre `workflow_status` (UPDATE ... WHERE estado esperado, rowcount), inserción+lectura de `content_reviews`, inserción+lectura ASC de `workflow_events` (sin rutas UPDATE/DELETE), lectura de la decisión vigente por (item, versión enviada), query de cola (ítems `PENDING_CONTENT_REVIEW` de marcas con membresía, orden por evento `SUBMITTED` más reciente ASC con desempate `id` ASC) y tests: claim concurrente deja una sola transición, cola aislada entre marcas y con orden determinista, history ASC y sin rutas de mutación (auditoría del repositorio).

  **Nota:** la clase se llamó `InvalidWorkflowTransitionError` (no `InvalidReviewTransitionError`) para reutilizar el mismo nombre que `app.brand_dna.policies`/`app.creative.policies` — `errors.py` ya distingue los tres por import alias, y así el patrón de registro de handlers queda idéntico en los tres módulos. El claim usa el mismo patrón `SELECT ... FOR UPDATE` (`lock_item`) que `app.creative.service`/`app.brand_dna.service` en vez de un `UPDATE ... WHERE` con rowcount crudo — SQLite no soporta `FOR UPDATE` de verdad, así que además se replicó el retry-on-`IntegrityError` de `creative.service.submit` para la carrera de dos decisiones concurrentes (verificado con un test de concurrencia real).
- [x] 2.2 Registrar handlers en `apps/api/app/errors.py` para `InvalidReviewTransitionError` → 409 con envelope `INVALID_WORKFLOW_TRANSITION` (mismo código que Brand DNA), manteniendo `PermissionDeniedError` → 403; verificar con un test de envelope por estado.

## 3. Service transaccional y router

- [x] 3.1 Crear `apps/api/app/governance/service.py` con los commands transaccionales de `design.md` (una transacción por command, sin `await` dentro): `submit` (rol Creator, versión pertenece al ítem, estado `DRAFT` o `CONTENT_CHANGES_REQUESTED`; reenvío exige versión de número mayor a la última enviada; transición + evento `SUBMITTED`), `approve` (rol Content Reviewer, claim desde `PENDING_CONTENT_REVIEW`, decisión + transición + evento `CONTENT_APPROVED`; read-back idempotente tras perder el claim: misma dirección → resultado vigente, dirección contraria → 409) y `request_changes` (feedback obligatorio, decisión `CHANGES_REQUESTED` + transición + evento `CONTENT_CHANGES_REQUESTED`, misma idempotencia). Verificar con tests service: happy path submit→changes→nueva versión→resubmit→approve; 403 Creator/Visual; 409 estados inválidos y misma versión reenviada; feedback vacío rechazado; idempotencia secuencial y concurrente.

  **Nota:** `submit` NO se reimplementó ni se movió a `governance` — `app.creative.service.submit` ya cumplía exactamente el contrato descrito aquí (congela `submitted_version_id` vía evento `SUBMITTED`, exige versión mayor en cada reenvío, idempotente) desde antes de esta change; `tests/test_creative_submit.py` incluso tenía un `force_status()` documentado como "governance (phase 5) owns the real transition", esperando solo approve/request_changes. `governance.service` solo añade `approve`/`request_changes`/`queue`/`detail`/`history`, reutilizando `last_submitted_version_id` de `app.creative.repository`.
- [x] 3.2 Crear `apps/api/app/governance/router.py` + `schemas.py` con los endpoints de `API.md`: `GET /content-reviews/queue`, `GET /content-reviews/{item_id}` (versión congelada completa + contexto aplicado persistido + feedback + estado, sin llamadas a IA), `POST /content-reviews/{item_id}/approve` (sin body), `POST /content-reviews/{item_id}/request-changes` (body estricto `{feedback}`), `GET /content-reviews/{item_id}/history` (ASC); y `POST /creative-items/{item_id}/submit` (body `{version_id}`) delegando al service; orden membership-first (403 → 404 → 403 rol → 409 estado → 422 validación); registrar router en `main.py`; verificar con tests API de queue/detalle/decisiones/history, envelopes 403/404/409/422 y que ninguna request acepta estado desde el cliente.

  **Nota:** `POST /creative-items/{item_id}/submit` ya vivía en `app.creative.router` (ver nota de 3.1) — no se movió. `/content-reviews/{item_id}` responde 200 con `submitted_version`/`applied_context`/`latest_review` en `null` cuando el ítem aún no fue enviado (en vez de 404), porque el Creator debe poder leer el estado de su propio ítem antes del primer submit (ver 4.2).

## 4. Frontend: Approvals y estado Creator

- [x] 4.1 Crear `apps/web/src/features/approvals/` con la página de cola (`/approvals`, conectada en `nav.ts` SOLO para `CONTENT_REVIEWER`), detalle de revisión (versión congelada en superficie editorial densa con clay tenue, contexto aplicado persistido, feedback previo, timeline append-only ASC) y acciones approve/request-changes con formulario de feedback validado (React Hook Form + Zod, patrón existente), server state con TanStack Query, estados reales de carga/vacío/error y manejo de 403/409 según el envelope; registrar la ruta en `routes.tsx`. Verificar con `npm run typecheck` y tests mínimos de gating de rol y render de estados.
- [x] 4.2 En la superficie de Creative Studio (feature de 008), añadir al Creator la vista de solo lectura de estado del workflow y feedback de decisiones (misma fuente del detalle de revisión: versión enviada, decisiones previas) sin ningún control de edición sobre la versión enviada; verificar que Approvals permanece oculta para Creator/Visual Reviewer y `npm run typecheck` pasa.

## 5. Verificación

- [ ] 5.1 Suite completa backend `uv run ty check && uv run pytest -q` (incluye tests de governance nuevos y las suites existentes sin regresión) y frontend `npm run typecheck && npm test` en `apps/web`; verificación manual con tokens dev alternando Creator/Content Reviewer: crear ítem, generar versión, submit, request changes con feedback, versión nueva, resubmit, approve, consultar history; confirmar que las decisiones no modifican el output enviado y que el Creator no ve Approvals.

  **Estado:** backend `uv run ty check && uv run pytest -q` limpio (331 passed; las 3 fallas presentes son preexistentes y no relacionadas — env local con secretos reales de OpenAI/Langfuse filtrando en tests de resolver que no fijan `Settings()` completo). Frontend `npm run typecheck && npm test` limpio (26 passed). Verificación manual con tokens dev NO ejecutada todavía: requiere aplicar la migración a la Supabase real del proyecto (`a73adfcae836` + `a31264bda9ea`) y correr `scripts/seed.py`/`scripts/dev_tokens.py` contra ella, lo cual se dejó pendiente de confirmación explícita del usuario (blast radius: escritura en la base de datos de producción real, aunque sea demo data idempotente). No marcar completada hasta ejecutar ese paso.
