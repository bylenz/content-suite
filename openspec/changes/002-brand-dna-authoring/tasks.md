## 1. Persistencia y seeds

- [x] 1.1 Crear modelos SQLAlchemy y migración Alembic de `brand_dna_versions` con enums, `UNIQUE(brand_id, version)` e índices únicos parciales de `DRAFT` y `ACTIVE` por marca; verificar `upgrade`/`downgrade`/`upgrade` sobre base limpia con `uv run`.
- [x] 1.2 Crear script de seeds idempotente (marca `Kinu`, perfiles demo y membresías por rol) ejecutable con `uv run`; verificar que repetirlo no duplica datos y que `/api/v1/me` resuelve las membresías.

## 2. API del módulo brand_dna

- [x] 2.1 Crear el módulo `app/brand_dna` (router, service, repository, policies, schemas) conectado a la app, reutilizando la dependencia de auth y `authorize_brand_action`; verificar la layering sin SQL en router y lint/typecheck con `uv run`.
- [x] 2.2 Implementar el contrato canónico del documento en Pydantic (`extra="forbid"`, tipos, cardinalidades, límites, recorte de espacios, tamaño máximo) y la derivación de conteos por sección; verificar en tests el 422 con rutas de campo por sección faltante, tipo inválido, campo desconocido y string vacío tras recorte.
- [x] 2.3 Implementar `GET /api/v1/brands/{brand_id}/brand-dna`, `GET .../brand-dna/versions` y `GET .../brand-dna/versions/{version}` según la matriz RBAC de lectura; verificar en tests que revisores reciben `draft: null`, lista sin entradas `DRAFT` y 404 al solicitar una versión en `DRAFT`, que el Creator obtiene su borrador completo y que todo miembro lee versiones publicadas con documento íntegro.
- [x] 2.4 Implementar `PATCH /api/v1/brands/{brand_id}/brand-dna/draft` (upsert del borrador único con `SELECT ... FOR UPDATE` sobre la marca antes de calcular `max(version)+1`, inicialización desde la versión activa, inmutabilidad de `ACTIVE`/`ARCHIVED`, transición `SYNCED -> OUTDATED` de la `ACTIVE` vigente, recuperación determinista de `IntegrityError` con rollback y relectura bajo bloqueo); verificar en tests la creación de v1, el nuevo borrador sobre `ACTIVE`, la marca `OUTDATED` y su idempotencia, el 403 de revisores y dos escrituras simultáneas del borrador (un único `DRAFT`, sin errores de servidor).
- [x] 2.5 Implementar `POST /api/v1/brands/{brand_id}/brand-dna/publish` transaccional con `expected_draft_id` requerido y la secuencia obligatoria `SELECT ... FOR UPDATE` por marca -> cargar borrador y activa -> `ACTIVE` previa `-> ARCHIVED` + flush -> `DRAFT -> ACTIVE` + `published_at` + `knowledge_status = NOT_SYNCED` -> flush + commit; verificar en tests el reintento idempotente, el 409 por `expected_draft_id` desactualizado o sin versiones, el mapeo determinista de `IntegrityError` y una publicación concurrente (dos solicitudes paralelas) sin doble `ACTIVE` ni 5xx.
- [x] 2.6 Implementar el envelope de error centralizado `{error: {code, message, details}}` con manejadores de app que traduzcan `HTTPException` (incluidos identity y health), `RequestValidationError` y errores de dominio, con códigos 401 `UNAUTHENTICATED`, 403 `PERMISSION_DENIED`, 404 `NOT_FOUND`, 409 `INVALID_WORKFLOW_TRANSITION`, 422 `VALIDATION_ERROR` y 503 `SERVICE_UNAVAILABLE` (health readiness sin base de datos y auth sin configurar); verificar en tests la forma exacta del envelope para 401, 403, 404, 409, 422 y 503.

## 3. Frontend

- [x] 3.1 Implementar la utilidad de dev que acuña tokens de vida corta por perfil seed y escribe el mapping ignorado rol -> token (`apps/web/.env.development.local`), y el adjunto del token solo en builds de desarrollo; verificar que el shell autentica únicamente tras `GET /api/v1/me`, que identidad y rol mostrados provienen solo de esa respuesta, que un token inválido o ausente deja la sesión anónima, que el cambio de identidad invalida las queries de marca y que el token no aparece en logs ni en el repositorio.
- [x] 3.2 Implementar la integración Dashboard <-> Brand DNA: panel de estado azul profundo con badge, conteos por sección, estado de Knowledge y CTA de onboarding en vacío; verificar los datos contra la API real y los estados de carga/error.
- [x] 3.3 Implementar la superficie Brand DNA: navegación de secciones (Overview + cinco secciones + Versiones), vista editorial del documento, edición del borrador, publicación con `expected_draft_id` y manejo del 409, y versiones; verificar el flujo Creator desde edición hasta publicación y la presentación honesta de `NOT_SYNCED`/`OUTDATED`.
- [x] 3.4 Implementar la superficie Create Brand DNA con React Hook Form + Zod (validadas con Context7 antes de instalar) usando el esquema espejo estricto del contrato canónico; verificar la creación manual de un documento válido y los errores de validación por sección con mensajes por campo.
- [x] 3.5 Implementar modo solo lectura para revisores y estados de carga, vacío, error de API y falta de permiso en todas las superficies nuevas; verificar que no existen controles de escritura para revisores, que ninguna vista muestra datos de borradores a revisores y que las secciones del starter fuera de alcance siguen deshabilitadas.
- [x] 3.6 Verificar responsive desktop/móvil, foco visible con contraste >= 3:1 y `prefers-reduced-motion` según la referencia fluffy y `starter-design/`; documentar cualquier desviación como deuda explícita.

## 4. Verificación y documentación

- [x] 4.1 Completar la suite pytest de brand_dna: happy path de publicación y re-versionado, permisos por rol (403) y denegación de borradores a revisores, membresía ausente, autenticación (401), transiciones inválidas y conflictos de publicación (409), validación canónica (422), transición `OUTDATED`, concurrencia de publicación y de escritura simultánea del borrador; verificar `uv run pytest` completo.
- [x] 4.2 Ejecutar lint, typecheck y tests desde la raíz (`npm run lint`, `npm run typecheck`, `npm run api:lint`, `npm run api:typecheck`, `npm run api:test`) y el build web; verificar todo en verde.
- [x] 4.3 Ejecutar la revisión visual desktop y móvil y el detector de Impeccable sobre los archivos UI modificados; resolver findings o registrarlos como deuda explícita.
- [x] 4.4 Actualizar `README.md` con el minting de tokens de dev de vida corta, el mapping ignorado, los seeds y los comandos locales exactos de la capability; verificar la conectividad web-API autenticada end-to-end documentada.
