## 1. Migración

- [x] 1.1 Crear `apps/api/alembic/versions/<rev>_supabase_rls_hardening.py` con `down_revision = "c4a9b2e6f8d1"` y rama por dialecto según `design.md`: en PostgreSQL, `ENABLE ROW LEVEL SECURITY` en `brands`, `profiles`, `brand_memberships`, `brand_dna_versions`, `brand_knowledge_chunks` y las cinco políticas `FOR SELECT TO authenticated` (`rls_profiles_select_self`, `rls_brand_memberships_select_own`, `rls_brands_select_members`, `rls_brand_dna_versions_select_members`, `rls_brand_knowledge_chunks_select_members`), cada `CREATE POLICY` precedido de `DROP POLICY IF EXISTS`; en SQLite, no-op explícito documentado; downgrade: drop de políticas + `DISABLE ROW LEVEL SECURITY` (PostgreSQL) / no-op (SQLite); sin `FORCE ROW LEVEL SECURITY` y sin tocar el esquema `auth`; documentar en el docstring que `service_role` es bypass de RLS y secreto de servidor; verificar con `uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` sobre SQLite y `uv run ty check`.
  - Resultado: migración `597b7798962f_supabase_rls_hardening.py` (sentencias desde constantes de módulo `DOMAIN_TABLES`/`READ_POLICIES`; rama SQLite con early-return documentado; downgrade espejo). Round-trip SQLite aislado (`sqlite:////tmp/...`): `upgrade head` (→ `597b7798962f`) → `downgrade -1` (→ `c4a9b2e6f8d1`) → `upgrade head`, sin errores; `ruff check` y `ty check` limpios.
- [x] 1.2 Verificar con Context7/documentación oficial de Supabase/PostgreSQL (RLS denegación por defecto sin políticas, `TO authenticated` vs `PUBLIC`, bypass del dueño sin `FORCE`, `service_role` con bypass, evaluación de subconsultas de políticas con los permisos del rol que consulta) y registrar versión/fuentes y cualquier ajuste en esta misma tarea.
  - Decisión (sin ajustes; diseño validado): Context7 `/supabase/supabase` (docs oficiales: `guides/database/postgres/row-level-security.mdx`, `guides/getting-started/api-keys.mdx`, `guides/api/securing-your-api`) confirma: (1) cada clave mapea a un rol Postgres real (`anon`/`authenticated`/`service_role`); (2) `service_role` tiene `BYPASSRLS` — las políticas nunca aplican a la secret key, que es secreto de servidor; (3) Postgres evalúa primero los grants de tabla y luego RLS (falta de grant → error de permiso; política sin filas → resultado vacío), por lo que no se añaden grants en esta migración: la denegación/aislamiento la expresa RLS y la evidencia de grants queda en 3.3; (4) `auth.uid()` extrae el uid del JWT de la petición (contrato `auth.uid() = profiles.id`). Semántica PostgreSQL (docs `CREATE POLICY`/RLS, docs.postgresql.org §5.8 y referencia `CREATE POLICY`): (5) sin política aplicable a un rol, RLS habilitado implica denegación por defecto (cero filas) — `anon` sin política → cero filas vía Data API; (6) omitir `TO` hace la política `PUBLIC` (incluye `anon`) — por eso cada `CREATE POLICY` lleva `TO authenticated` explícito; (7) superusuarios, roles `BYPASSRLS` y — sin `FORCE ROW LEVEL SECURITY` — el dueño de la tabla no están sujetos a RLS (la migración nunca usa `FORCE`; el bypass del dueño se verifica en 3.3, no se asume); (8) las subconsultas de una política se evalúan con los permisos del rol que consulta, sin recursión de las políticas de las tablas referenciadas — el `EXISTS` de auto-membresía compone cerrado con `rls_brand_memberships_select_own`. El enfoque del diseño quedó validado; ningún ajuste necesario.

## 2. Tests offline

- [x] 2.1 Crear `apps/api/tests/test_rls_migration_sql.py` (patrón `test_knowledge_postgres_sql.py` de 006, sin red ni instancia) verificando de la rama PostgreSQL de la migración: RLS habilitado exactamente en las cinco tablas de dominio (y en ninguna otra), las cinco políticas con sus predicados (`id = auth.uid()`, `profile_id = auth.uid()`, y los tres `EXISTS` con `m.profile_id = auth.uid()`), todas `FOR SELECT`, todas `TO authenticated` (ninguna aplica a `PUBLIC`/`anon`), ninguna política de escritura (`INSERT`/`UPDATE`/`DELETE`/`ALL`), idempotencia (`DROP POLICY IF EXISTS` previo a cada `CREATE POLICY`), rama SQLite no-op y el invariante anti-recursión con aserción dedicada: ningún predicado `USING` referencia la tabla que su política protege y las políticas de marca referencian únicamente `brand_memberships` (grafo de referencias acíclico), explícitamente además del pin exacto de la secuencia; ejecutar `uv run pytest tests/test_rls_migration_sql.py`.
  - Resultado: 8 tests SQL ejecutan `upgrade()`/`downgrade()` de la migración cargada por ruta con un `op` grabador por dialecto (sin red ni instancia): igualdad exacta de la secuencia completa PostgreSQL (5 `ENABLE` + 5 pares DROP/`CREATE POLICY` con predicados literales), RLS solo en las cinco tablas de dominio, todas `FOR SELECT TO authenticated` (rechaza `INSERT`/`UPDATE`/`DELETE`/`ALL`/`TO PUBLIC`/`TO anon`/`FORCE`), `DROP POLICY IF EXISTS` inmediatamente previo a cada `CREATE POLICY`, downgrade espejo, sin DDL sobre el esquema `auth` (solo uso de la función `auth.uid()`), rama SQLite no-op en ambas direcciones, y aserción dedicada del invariante anti-recursión (`test_pg_policy_using_never_queries_the_table_it_protects`): ningún predicado `USING` referencia como fuente de consulta (`FROM`) la tabla que su política protege, y las políticas de marca referencian únicamente `brand_memberships` (grafo de referencias acíclico), además del pin exacto de la secuencia → 10 passed (8 SQL + 2 de contrato de identidad).
- [x] 2.2 Añadir test de contrato de identidad: `app/identity/auth.py` sigue usando el `sub` verificado del JWT como `AuthenticatedUser.id` y los servicios lo resuelven como PK de `profiles` (mapeo `auth.uid() = profiles.id` del que dependen las políticas), sin aceptar roles ni membresías del cliente; verificar con `uv run pytest tests/` completo en `apps/api`.
  - Resultado: 2 tests en el mismo archivo: un JWT con claims de rol/membresía falsos del cliente (`role`, `user_metadata.brand_memberships`) devuelve `AuthenticatedUser.id == sub` y solo expone los campos `{id, email}`; y `get_profile`/`get_membership` resuelven por esa misma id como PK de `profiles` server-side. Suite completa: `uv run pytest` → **236 passed** (227 previas + 9 nuevas), sin modificación de tests existentes.

## 3. Calidad y validación

- [x] 3.1 Ejecutar `uv run ruff check`, `uv run ty check` y `uv run pytest` en `apps/api`; confirmar con auditoría estática que ningún archivo de aplicación cambió (solo nueva migración + tests) y que la suite existente (incluida 005/006) pasa sin modificaciones de comportamiento.
  - Resultado: `ruff check .` → All checks passed; `ty check` → All checks passed (2 hallazgos iniciales en el test nuevo corregidos: atributo del módulo de migración anotado como `Any` y `Profile | None` afirmado antes de `.id`); `pytest` → **236 passed** (suite 005/006 intacta, +9 tests nuevos, cero modificaciones a tests existentes). Auditoría por mtime contra la migración como referencia temporal: los únicos archivos escritos por esta change son `alembic/versions/597b7798962f_supabase_rls_hardening.py` (nuevo), `tests/test_rls_migration_sql.py` (nuevo) y `openspec/changes/007-supabase-rls-hardening/tasks.md`; ningún archivo de `apps/api/app/` (ni frontend, ni dependencias) fue modificado.
- [x] 3.2 Ejecutar `npx @fission-ai/openspec@latest validate 007-supabase-rls-hardening --strict` y `git diff --check` ANTES de cualquier DDL de producción: la validación estricta y el diff limpio son precondición de la tarea 3.3; dejar la change sin archivar.
  - Resultado: `validate 007-supabase-rls-hardening --strict` → "Change '007-supabase-rls-hardening' is valid"; `git diff --check` → limpio (sin whitespace errors). Change sin archivar; 3.3 permanece pendiente (verificación manual de producción, fuera del alcance de esta implementación).
- [x] 3.3 Verificación manual de producción (Supabase), documentando resultado o pendiente sin marcar si no se ejecutó, en este orden:
  - Resultado (2026-09-14, proyecto Supabase `ContentSuite` / `fonhwfvzrxxxmetczspz`, región `sa-east-1`, recién creado): el caso real difiere del previsto en el diseño — el proyecto estaba completamente vacío (`public` sin tablas, `to_regclass('public.alembic_version')` NULL, extensión `vector` no instalada), no un esquema `c4a9b2e6f8d1` preexistente aplicado fuera de Alembic. El gate de paridad de esquema no aplica a este caso (no hay DDL preexistente contra el cual comparar); se ejecutó la cadena completa desde cero. El head actual del repo (`a3d8f2c6e9b1`, incluye changes 008/009 ya presentes como archivos sin commitear) va más allá del alcance de 007, así que se aplicó hasta la revisión RLS explícitamente (`uv run alembic upgrade 597b7798962f`, no `head`), evitando desplegar código fuera de esta change.
    - **Propietario y rol** (pre-aplicación): `CONTENT_SUITE_DATABASE_URL` (agregado por el usuario a `apps/api/.env`, gitignored) conecta como `postgres` (`current_user = current_role = session_user = postgres`, verificado con una conexión SQLAlchemy real, no vía MCP). Tras aplicar, `pg_tables.tableowner = postgres` en las cinco tablas → el rol de aplicación es el propietario (y superusuario) → bypass de RLS confirmado, no asumido.
    - **Aplicación**: `uv run alembic upgrade 597b7798962f` corrió `dec022b4c864 → b7d1e4c2a9f3 → c4a9b2e6f8d1 → 597b7798962f` sin errores (base vacía, no había nada que reconciliar); `uv run alembic current` confirma `597b7798962f`.
    - **Catálogo**: `pg_class.relrowsecurity = true` y `relforcerowsecurity = false` en las cinco tablas de dominio (ninguna otra tabla en `public`, porque no existe ninguna otra). `pg_policies` devuelve exactamente las cinco políticas (`rls_profiles_select_self`, `rls_brand_memberships_select_own`, `rls_brands_select_members`, `rls_brand_dna_versions_select_members`, `rls_brand_knowledge_chunks_select_members`), todas `{authenticated}`/`SELECT`, con predicados `qual` idénticos a `design.md`.
    - **Grants**: `information_schema.role_table_grants` muestra que `anon` y `authenticated` tienen `SELECT/INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER` otorgados a nivel de tabla en las cinco tablas — comportamiento de bootstrap por defecto de Supabase (`ALTER DEFAULT PRIVILEGES` del propietario `postgres` al crear el proyecto), no algo que esta migración añade (no toca grants, tal como documenta `design.md`). **Divergencia frente a la expectativa del diseño**: `has_table_privilege('anon'|'authenticated', '<tabla>', 'TRUNCATE')` da `true` en las cinco tablas, no `false` como esperaba la tarea. Riesgo real acotado: `TRUNCATE` no es una operación de PostgREST/Data API (no hay endpoint que la invoque) y `anon`/`authenticated` son roles `NOLOGIN` en Supabase (no se puede abrir una conexión Postgres directa autenticada como esos roles sin ya tener acceso privilegiado a la base) — el grant es alcanzable solo con una conexión Postgres directa ya privilegiada, no vía la superficie que esta change protege. No remediado en esta migración (que deliberadamente no toca grants); queda como decisión pendiente para el usuario: opcionalmente, una change futura podría `REVOKE TRUNCATE ON <tablas> FROM anon, authenticated` como capa adicional de defensa en profundidad.
    - **Denegación/aislamiento con claims JWT válidos**: prueba transaccional real (`BEGIN`; filas de prueba insertadas como `postgres`; `SET LOCAL ROLE authenticated` + `SET LOCAL request.jwt.claims` con `sub` real; conteos capturados en una tabla temporal; `ROLLBACK` final, cero persistencia verificada post-transacción) — resultados: `anon` ve 0 filas en `brands`/`profiles`/`brand_memberships`; un `sub` con membresía ve 1 fila en `brands` (la suya) y 1 en `brand_memberships`/`profiles` (las suyas); un `sub` sin membresía ve 0 en `brands`/`brand_memberships` pero 1 en `profiles` (su propia fila, política `id = auth.uid()`, comportamiento esperado, no una fuga). Confirma exactamente la matriz de aislamiento esperada.
    - **Rol de aplicación**: lectura y escritura confirmadas con la conexión real de `CONTENT_SUITE_DATABASE_URL` (INSERT + UPDATE + SELECT dentro de una transacción con `ROLLBACK`, cero filas persistidas).
    - **Supabase Security Advisor**: sin hallazgos de RLS faltante en las cinco tablas de dominio; único hallazgo (`extension_in_public` para `vector`, WARN) preexistente de la migración 006 (`c4a9b2e6f8d1`), fuera del alcance de 007.
    - Vía usada: directa con Alembic (preferida), sin necesidad de estampado manual por tratarse de una base vacía.
  - Seguimiento del hallazgo de TRUNCATE (2026-09-14, decisión del usuario: cerrarlo ahora): migración adicional `e3dfa0780566_revoke_truncate_anon_authenticated.py` (`down_revision = "597b7798962f"`, rama por dialecto igual que `597b7798962f`; PostgreSQL: `REVOKE TRUNCATE ON TABLE <tabla> FROM anon, authenticated` en las cinco tablas, sin tocar `SELECT/INSERT/UPDATE/DELETE`; SQLite no-op; downgrade `GRANT TRUNCATE` espejo). Branchea directamente sobre `597b7798962f` (no sobre el head real del repo `a3d8f2c6e9b1`) para no arrastrar el despliegue de las changes 008/009, aún sin terminar/revisar — head múltiple documentado en el docstring, a reconciliar con una migración de merge cuando esas changes cierren. Test offline `tests/test_truncate_revoke_migration_sql.py` (mismo patrón que `test_rls_migration_sql.py`: `op` grabador, sin red): secuencia exacta de `REVOKE`, ningún otro privilegio tocado, downgrade espejo, rama SQLite no-op → 4 tests. `ruff check`, `ty check` limpios (los 10 diagnósticos de `ty check` en el árbol pertenecen a `tests/test_creative_submit.py`/`tests/test_openai_text.py`, archivos sin commitear de las changes 008/009, no tocados por esta migración). Round-trip SQLite aislado (`upgrade e3dfa0780566 → downgrade -1 → upgrade e3dfa0780566`) sin errores. Suite completa `uv run pytest` → 317 passed (RLS: 10 + 4 nuevos + resto). Aplicada a producción con `uv run alembic upgrade e3dfa0780566`; verificación post-aplicación: `has_table_privilege('anon'|'authenticated', '<tabla>', 'TRUNCATE')` → `false` en las cinco tablas (antes `true`); `has_table_privilege(..., 'SELECT')` sigue `true` a nivel de grant (RLS sigue siendo el gate real de filas, sin cambios en las políticas). Hallazgo cerrado.
  - **Pre-aplicación — ledger y paridad**: inspeccionar `SELECT version_num FROM alembic_version` (o equivalente) en producción; si la tabla no existe o está vacía mientras las cinco tablas de dominio ya existen (esquema `c4a9b2e6f8d1` aplicado fuera de Alembic), NO estampar directamente: ejecutar primero el **gate de paridad de esquema** (design.md «Verificación de paridad de esquema») recogiendo evidencia concreta contra la cadena Alembic 001–006 — columnas vía `information_schema.columns` (tipo, nullabilidad, defaults), enums vía `pg_enum` (valores y orden), índices vía `pg_indexes` (definición exacta, incluidos los parciales), constraints vía `pg_constraint`/`information_schema.table_constraints` (nombres exactos, incluido el FK truncado `fk_brand_knowledge_chunks_brand_dna_version_id_brand_dn_026c`), extensión `vector` y `embedding` sin dimensión fija (`format_type`/`atttypmod = -1`) —; cualquier diferencia → alto seguro: no estampar, documentar diferencia y remediación, reintentar tras corregir; solo con paridad exacta estampar `c4a9b2e6f8d1` por una vía soportada sin reproducir el DDL existente (directa con Alembic — `uv run alembic stamp c4a9b2e6f8d1` con `CONTENT_SUITE_DATABASE_URL` de Supabase —, preferida; o Supabase MCP con SQL equivalente creando `alembic_version` con el esquema exacto de Alembic); si la fila existe y difiere de la cabeza esperada, alto y diagnóstico manual sin estampar a ciegas; registrar en esta tarea la evidencia de paridad y la vía usada.
  - **Pre-aplicación — propietario y rol**: consultar `SELECT tablename, tableowner FROM pg_tables WHERE schemaname = 'public' AND tablename IN ('brands','profiles','brand_memberships','brand_dna_versions','brand_knowledge_chunks')` y el rol real de la conexión de la aplicación (`SELECT current_user, current_role` en esa conexión); si el rol de la aplicación no es el propietario de las tablas, detener sin aplicar DDL y documentar la remediación (reconexión con rol propietario/`service_role` de servidor o transferencia de propiedad) antes de reintentar; no asumir el bypass del dueño.
  - **Aplicación**: `uv run alembic upgrade head` con `CONTENT_SUITE_DATABASE_URL` de Supabase (o DDL vía MCP avanzando `alembic_version` a la nueva revisión en la misma operación); verificar que solo se ejecuta la revisión RLS.
  - **Post-aplicación — catálogo y grants**: confirmar vía `pg_tables.relrowsecurity` y `pg_policies` RLS + cinco políticas; registrar evidencia de grants con `SELECT grantee, table_name, privilege_type FROM information_schema.role_table_grants WHERE table_schema = 'public' AND table_name IN (...)` (o `has_table_privilege`) para `anon`, `authenticated` y el rol de la aplicación, y evidencia explícita de que ni `anon` ni `authenticated` tienen `TRUNCATE` en ninguna de las cinco tablas (`SELECT has_table_privilege('anon', 'public.<tabla>', 'TRUNCATE')` y su equivalente para `authenticated`, esperado `false`): RLS no gobierna `TRUNCATE` (operación a nivel de tabla regida por grants), por lo que su ausencia se verifica por grants, no por políticas.
  - **Post-aplicación — denegación/aislamiento con claims válidos**: leer como `authenticated` con un contexto de claims JWT válido para que `auth.uid()` no sea NULL (sin claims, `auth.uid()` es NULL y el resultado de cero filas sería vacuo, no evidencia), p. ej. `BEGIN; SET LOCAL ROLE authenticated; SET LOCAL request.jwt.claims = '{"sub":"<profile_id real>","role":"authenticated"}'; SELECT count(*) FROM brands; ROLLBACK;` o el equivalente soportado por el cliente disponible, comprobando: un `sub` con membresía ve únicamente filas de su(s) marca(s) (>0), un `sub` sin membresía ve 0 filas por decisión de política, y `anon` no ve nada.
  - **Post-aplicación — rol de aplicación**: evidencia de lectura y escritura con el rol de conexión de la aplicación (transacción de prueba con `ROLLBACK` o flujo real de la API observado) confirmando el bypass de dueño; registrar la evidencia en esta tarea.
