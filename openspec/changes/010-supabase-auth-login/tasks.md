## 1. Configuración y dependencias

- [x] 1.1 En el dashboard de Supabase del proyecto real: habilitar el proveedor de email/magic link y registrar las redirect URLs de dev (`http://localhost:5173`) y de producción; verificar consultando la configuración de Auth del proyecto. — Confirmado indirectamente: `signInWithOtp` contra el proyecto real acepta un email válido (`https://fonhwfvzrxxxmetczspz.supabase.co/auth/v1/otp` responde 200), lo que solo ocurre con el proveedor de email/magic link habilitado. La redirect URL de producción sigue pendiente de registrar cuando exista un dominio real de despliegue.
- [x] 1.2 Consultar Context7 para la versión estable de `@supabase/supabase-js` compatible con Vite 8/React 19 e instalarla en el workspace web (`npm install --workspace @content-suite/web`); verificar que `apps/web/package.json` y el lockfile raíz reflejan la dependencia. — Instalada `@supabase/supabase-js@2.116.0` (versión estable más reciente, Node >=22, sin peer deps de React).
- [x] 1.3 Añadir `VITE_SUPABASE_URL` y `VITE_SUPABASE_ANON_KEY` a `apps/web/.env.example` (sin valores reales) y documentar su origen (Supabase dashboard → Project Settings → API); verificar que `apps/web/.env.development.local` del propio desarrollador las define para pruebas locales.

## 2. Cliente Supabase y sesión

- [x] 2.1 Crear `apps/web/src/features/session/supabaseClient.ts` que instancie `createClient` una sola vez a partir de `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`; verificar con un test unitario que lanza un error claro si falta alguna env var en un build de producción. — `createSupabaseClient(env)` es una factory pura y testeable (`supabaseClient.test.ts`, 3 tests): lanza en producción sin config, devuelve `null` fuera de producción sin config (dev/test degradan sin crashear).
- [x] 2.2 Extender `SessionProvider` para suscribirse a `supabase.auth.onAuthStateChange`, llamar `setApiAuthToken(session?.access_token ?? null)` en cada evento y reutilizar el `GET /api/v1/me` + `toProfile` existentes para poblar `SessionProfile`; verificar con un test de React Testing Library que una sesión emitida por el listener produce `status === 'authenticated'`. — Cubierto por `SessionProvider.test.tsx`.
- [x] 2.3 Añadir `signInWithMagicLink(email)` y `signOut()` al contrato de `SessionContext` (junto a `authenticate`/`switchRole` existentes); verificar con tests que ambas funciones delegan en el cliente Supabase y no tocan `devTokens.ts`. — Cubierto por `SessionProvider.test.tsx`.
- [x] 2.4 Al montar, resolver la sesión existente de Supabase antes de decidir `status: 'anonymous'`, preservando `status: 'initializing'` mientras se resuelve. — Implementado vía el evento `INITIAL_SESSION` de `onAuthStateChange` (dispara exactamente una vez por suscripción, con o sin sesión) en vez de un `getSession()` aparte: cubre el mismo caso sin una segunda llamada al SDK ni el riesgo de carrera entre ambas. Cubierto por el test "con sesión de Supabase en INITIAL_SESSION, se autentica vía GET /me".

## 3. UI de login

- [x] 3.1 Extender la pantalla de login (`SessionMissingState`/`RequireSession`) con un formulario de email que llama `signInWithMagicLink` y muestra un estado de "revisa tu correo" tras el envío, sin revelar si el email existe; verificar manualmente en el navegador y con un test de componente para el estado post-envío. — Verificado en el navegador contra el proyecto Supabase real (ver 4.3) y con `StateViews.test.tsx`.
- [x] 3.2 Mostrar los accesos de identidad de desarrollo (`availableDevRoles`) únicamente cuando existan y siempre junto al formulario de login real, nunca en su lugar; verificar con un test que en un build simulado sin dev roles solo se renderiza el formulario real. — Cubierto por `StateViews.test.tsx` y confirmado visualmente en dev (ambos) y en el build de producción servido con `vite preview` (solo el formulario real, ver 4.4).
- [x] 3.3 Añadir un control de cierre de sesión visible para un usuario autenticado (shell/nav) que invoque `signOut()`; verificar manualmente que tras cerrar sesión la app vuelve a la pantalla de login y `apiFetch` dejó de enviar `Authorization`. — Añadido a `SidebarContent`; verificado manualmente en el navegador (login con identidad de dev → "Cerrar sesión" → vuelve a la pantalla de login).

## 4. Tests y verificación

- [x] 4.1 Ejecutar `npm run test --workspace @content-suite/web` y confirmar que la suite de `features/session` pasa completa, incluyendo los tests nuevos de 2.x y 3.x. — 38/38 tests, 8 archivos.
- [x] 4.2 Ejecutar `npm run typecheck --workspace @content-suite/web` y confirmar cero errores tras añadir el tipo del cliente Supabase. — Sin errores (`tsc -b`); también `npm run lint` sin errores.
- [~] 4.3 Verificación manual end-to-end contra el proyecto Supabase real: solicitar magic link con un email de prueba, confirmar el enlace, recargar la página (la sesión persiste), cerrar sesión (vuelve al login); registrar el resultado en el resumen de la change. — Verificado: (a) email inválido rechazado por Supabase con el mensaje mostrado inline sin crash; (b) email válido aceptado, Supabase responde 200 y la UI muestra "Revisa tu correo"; (c) login con identidad de dev + cierre de sesión funciona end-to-end. **Pendiente**: confirmar el enlace recibido y verificar que la sesión sobrevive a una recarga de página, que requiere acceso a una bandeja de correo real — no se pudo completar en este entorno.
- [x] 4.4 Confirmar manualmente que un build de producción (`npm run build --workspace @content-suite/web` servido con `vite preview`) no incluye ningún acceso de identidad de desarrollo en el HTML/JS servido. — Verificado: con las env vars de Supabase configuradas y sin las de dev tokens, el build de producción servido con `vite preview` muestra únicamente el formulario de login real; sin botones de identidad de dev ni el texto sobre tokens de desarrollo.
