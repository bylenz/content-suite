"""Observability: async tracing port with a safe no-op default, an optional,
environment-gated Langfuse adapter, the local sanitized read model and the
authorized facade over it. Tracing never alters domain state."""
