"""AI Platform: provider-agnostic model ports, structured contracts, versioned
prompt registry and the capability runner. Never imports provider SDKs outside
`providers/` and never changes workflow states."""
